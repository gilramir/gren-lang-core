// Outbound's row on JavaScript (geng-lang m2-interop.md D428). Nothing is
// converted on JavaScript, as D200 has it for every js row, so the value is
// the term. The row is here so that a program that imports `Outbound` still
// serves the js target.
function out(value) {
  return value;
}
