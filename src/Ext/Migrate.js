// Migrate's row on JavaScript (geng-lang m2-beam-toptier.md D458). Nothing is
// upgraded in place on JavaScript, so the term is the value. The row is here
// so that a program that imports `Migrate` still serves the js target.
function check(where, term) {
  return term;
}
